目录
---- 1.配置DNS
	服务器有时会出现无法连接外网的情况，此时使用此操作

---- 2.添加用户和删除用户
	创建一个新的用户，或者删除一个用户
	
---- 3. Anaconda 基础安装tensorflow环境 + jupyter
	安装虚拟环境的操作，如果虚拟环境损坏，按此操作重装
	
---- 4. 配置用户 sudo权限
	当用户需要使用sudu时，按此操作配置
	
---- 5. 启动分词系统服务器
	服务器崩溃，需要重启时按此操作
	
---- 6. 查看磁盘使用状态
	当服务器出现不明原因的异常，如操作失败时，首先查看硬盘使用情况
	
---- 7. 查看GPU使用状态
	当GPU无法发起任务时，查看gpu的状态

---- 8. 查看用户名

	
	
	
---------------------------------------------------------------------------------------------------
1. 配置dns
	在地址 https://laod.cn/hosts/2017-dns-pinggu.html 中
	随便选择一个如：
		阿里公共DNS是阿里巴巴集团推出的DNS递归解析系统，目标是成为国内互联网基础设施的组成部分，
		面向互联网用户提供“快速”、“稳定”、“智能”的免费DNS递归解析服务。
		DNS 服务器 IP 地址：
		首选：223.5.5.5
		备选：223.6.6.6

	#打开路由配置文件，找到相关语句
	vi /etc/resolv.conf
		# Dynamic resolv.conf(5) file for glibc resolver(3) generated by resolvconf(8)
		#     DO NOT EDIT THIS FILE BY HAND -- YOUR CHANGES WILL BE OVERWRITTEN
		nameserver 223.5.5.5
		nameserver 223.6.6.6
	之后保存即可
	
2. 添加用户和删除用户
	adduser username
	这样会创建一个没有分组的用户，命令执行后会要求输入一系列配置信息，输入完成后，即成功创建用户
	deluser username
	当创建错误时，输入该指令删除用户

3. Anaconda 基础安装tensorflow环境 + jupyter
	# 使用conda创建一个名字为 tensorflow 的 python版本为3.6的虚拟环境
	conda create -n tensorflow python=3.6
	# 启动虚拟环境
	source activate tensorflow
	# 安装包，-i https://pypi.tuna.tsinghua.edu.cn/simple 是python的国内源，如果速度慢的话就添加这个参数
	pip install -i https://pypi.tuna.tsinghua.edu.cn/simple tensorflow-gpu
	pip install -i https://pypi.tuna.tsinghua.edu.cn/simple ipython
	pip install -i https://pypi.tuna.tsinghua.edu.cn/simple jupyter
	
4. 配置用户 sudo权限
	配置步骤如下：
	
	1、登陆或切换到root用户下；

	2、添加sudo文件的写权限，命令是:chmod u+w /etc/sudoers

	3、编辑sudoers文件：vi /etc/sudoers

	找到这行 root ALL=(ALL) ALL,在他下面添加xxx ALL=(ALL) ALL (注：这里的xxx是你的用户名)

	你可以根据实际需要在sudoers文件中按照下面四行格式中任意一条进行添加：
	youuser            ALL=(ALL)                ALL
	%youuser           ALL=(ALL)                ALL
	youuser            ALL=(ALL)                NOPASSWD: ALL
	%youuser           ALL=(ALL)                NOPASSWD: ALL
	第一行：允许用户youuser执行sudo命令(需要输入密码)。
	第二行：允许用户组youuser里面的用户执行sudo命令(需要输入密码)。
	第三行：允许用户youuser执行sudo命令,并且在执行的时候不输入密码。
	第四行：允许用户组youuser里面的用户执行sudo命令,并且在执行的时候不输入密码。

	4、撤销sudoers文件写权限,命令:chmod u-w /etc/sudoers

	至此，普通用户就可以使用sudo了。
	
5. 启动分词系统服务器
	# 进入pipenv虚拟环境目录
	cd /mnt/pyVRenv/Atol_python3.6
	# 启动pipenv 命令行
	pipenv shell
	# 执行 gotoserver.sh 脚本进入服务器目录
	. gotoserver.sh
	# 进入分词系统服务器目录 ATol
	cd ATol
	# 初始化环境变量
	. boot.sh
	# 启动服务器 并输出日志到 > 后的目录
	nohup flask run --host=0.0.0.0 > /mnt/log/Atol/out.20190318 &

6. 查看磁盘使用状态
	# 查看当前系统硬盘的总体使用情况
	df -h
	# 查看目标目录的下每个文件夹的占用空间
	du -ah --max-depth=1 目标目标
	
7. 查看GPU使用状态
	# 首先启动 tensorflow 虚拟环境
	source activate tensorflow
	# 查看GPU状态1(更清晰的查询)
	gpustat
	# 查看GPU状态2
	nvidia-smi